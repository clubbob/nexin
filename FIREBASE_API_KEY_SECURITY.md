# Firebase API Key 제한 설정 가이드

Firebase API Key를 보호하기 위한 상세한 설정 가이드를 제공합니다.

## 🔒 API Key 제한 설정의 중요성

Firebase API Key는 클라이언트에서 사용되므로 완전히 숨길 수는 없지만, 제한을 설정하여 무단 사용을 방지할 수 있습니다.

## 📋 설정 방법

### 1단계: Google Cloud Console 접속

1. [Google Cloud Console](https://console.cloud.google.com) 접속
2. 상단 프로젝트 선택 드롭다운에서 **nexin-7290c** 프로젝트 선택
   - 또는 Firebase Console에서 프로젝트 설정 > 일반 > 프로젝트 ID 클릭

### 2단계: API 및 서비스 > 사용자 인증 정보

1. 왼쪽 메뉴에서 **API 및 서비스** 클릭
2. **사용자 인증 정보** 클릭
3. **API 키** 섹션에서 사용 중인 API Key 찾기
   - 키 이름: "브라우저 키" 또는 "웹 API 키"
   - 키 값이 `AIzaSy...`로 시작하는 항목

### 3단계: API Key 제한 설정

API Key를 클릭하여 설정 페이지로 이동합니다.

#### 3.1 애플리케이션 제한사항 설정

**"애플리케이션 제한사항"** 섹션에서:

1. **"HTTP 리퍼러(웹사이트)"** 선택
2. **"웹사이트 제한사항"** 섹션에서 **"항목 추가"** 클릭
3. 허용할 도메인 추가:

```
다음 형식으로 도메인 추가:

1. 프로덕션 사이트 (Vercel)
   - https://nexin.vercel.app/*
   - https://nexin-*.vercel.app/*
   - https://*.vercel.app/* (모든 Vercel 서브도메인 허용)

2. 로컬 개발 환경 (선택사항)
   - http://localhost:3000/*
   - http://127.0.0.1:3000/*

3. 커스텀 도메인 (있는 경우)
   - https://www.yourdomain.com/*
   - https://yourdomain.com/*
```

**도메인 추가 형식:**
- `https://example.com/*` - 모든 경로 허용
- `https://*.example.com/*` - 모든 서브도메인 허용
- `http://localhost:3000/*` - 로컬 개발용

**예시:**
```
https://nexin.vercel.app/*
https://nexin-*.vercel.app/*
http://localhost:3000/*
```

#### 3.2 API 제한사항 설정

**"API 제한사항"** 섹션에서:

1. **"키 제한"** 선택
2. **"API 선택"** 클릭
3. 다음 Firebase 관련 API만 선택:
   - ✅ Firebase Authentication API
   - ✅ Firebase Cloud Firestore API
   - ✅ Firebase Cloud Storage API
   - ✅ Firebase Realtime Database API (사용하는 경우)
   - ✅ Firebase Cloud Messaging API (사용하는 경우)

**중요:** 사용하지 않는 API는 선택하지 마세요. 최소 권한 원칙을 따르세요.

### 4단계: 저장 및 확인

1. 페이지 하단의 **"저장"** 버튼 클릭
2. 변경사항 적용까지 약 5분 소요될 수 있습니다
3. 저장 후 **"제한사항"** 열에서 설정 확인

## 🔍 설정 확인 방법

### 1. 제한된 도메인에서만 작동 확인

1. 허용된 도메인에서 사이트 접속
2. 개발자 도구(F12) > Console에서 Firebase 초기화 확인
3. 오류가 없으면 정상 작동

### 2. 제한된 도메인이 아닌 곳에서 차단 확인

1. 허용되지 않은 도메인에서 API Key 사용 시도
2. Google API에서 403 Forbidden 오류 발생
3. 이는 정상적인 보안 동작입니다

## 📝 단계별 스크린샷 가이드

### Google Cloud Console 접속
```
1. https://console.cloud.google.com 접속
2. 프로젝트 선택: nexin-7290c
```

### API 및 서비스 메뉴
```
왼쪽 메뉴:
└─ API 및 서비스
   └─ 사용자 인증 정보
      └─ API 키 섹션
         └─ 브라우저 키 클릭
```

### 애플리케이션 제한사항 설정
```
애플리케이션 제한사항:
○ 없음
○ IP 주소 (웹사이트에는 사용 안 함)
● HTTP 리퍼러(웹사이트) ← 선택

웹사이트 제한사항:
[항목 추가] 버튼 클릭
└─ 도메인 입력:
   - https://nexin.vercel.app/*
   - https://nexin-*.vercel.app/*
   - http://localhost:3000/*
```

### API 제한사항 설정
```
API 제한사항:
○ 키 제한 없음
● 키 제한 ← 선택

API 선택:
☑ Firebase Authentication API
☑ Firebase Cloud Firestore API
☑ Firebase Cloud Storage API
☐ (사용하지 않는 API는 체크 해제)
```

## ⚠️ 주의사항

### 1. 와일드카드 사용
- `https://*.vercel.app/*` - 모든 Vercel 서브도메인 허용
- `https://example.com/*` - example.com의 모든 경로 허용
- `https://*.example.com/*` - 모든 서브도메인 허용

### 2. 프로토콜 구분
- `http://`와 `https://`는 별도로 설정해야 합니다
- 프로덕션은 `https://`만 사용하세요

### 3. 로컬 개발 환경
- 로컬 개발을 위해 `http://localhost:3000/*` 추가
- 개발 완료 후 제거하거나 별도 개발용 API Key 사용 권장

### 4. 변경사항 적용 시간
- 설정 변경 후 즉시 적용되지 않을 수 있습니다
- 최대 5-10분 소요될 수 있습니다

## 🔄 API Key 교체 방법 (선택사항)

더 안전하게 하려면 새 API Key를 생성하고 기존 키를 삭제할 수 있습니다.

### 새 API Key 생성
1. Google Cloud Console > API 및 서비스 > 사용자 인증 정보
2. **"API 키 만들기"** 클릭
3. 키 이름 입력: "Nexin Web Key (New)"
4. 위의 제한 설정 동일하게 적용
5. 새 키를 `.env.local` 및 Vercel에 업데이트

### 기존 API Key 삭제
1. 기존 API Key 선택
2. **"삭제"** 버튼 클릭
3. 확인 후 삭제

## 🛡️ 추가 보안 조치

### 1. Firebase 보안 규칙 강화
- Firestore Rules 설정
- Storage Rules 설정
- Authentication 규칙 설정

### 2. 환경 변수 보호
- `.env.local` 파일은 절대 Git에 커밋하지 않기
- Vercel 환경 변수는 암호화되어 저장됨

### 3. 정기적인 키 검토
- 주기적으로 API Key 사용량 확인
- 의심스러운 활동 감지 시 즉시 키 교체

## 📞 문제 해결

### API Key가 작동하지 않는 경우

1. **제한 설정 확인**
   - 도메인이 정확히 입력되었는지 확인
   - 프로토콜(http/https) 확인
   - 와일드카드 사용 확인

2. **캐시 문제**
   - 브라우저 캐시 삭제
   - 시크릿 모드에서 테스트

3. **적용 시간 대기**
   - 설정 변경 후 5-10분 대기

### 403 Forbidden 오류

- 정상적인 보안 동작입니다
- 허용된 도메인에서만 접근 가능
- 제한 설정이 올바르게 작동하는 것입니다

## ✅ 체크리스트

- [ ] Google Cloud Console 접속
- [ ] nexin-7290c 프로젝트 선택
- [ ] API 및 서비스 > 사용자 인증 정보 이동
- [ ] API Key 찾기 및 클릭
- [ ] 애플리케이션 제한사항: HTTP 리퍼러(웹사이트) 선택
- [ ] 허용 도메인 추가 (Vercel, 로컬 등)
- [ ] API 제한사항: 키 제한 선택
- [ ] 필요한 Firebase API만 선택
- [ ] 저장
- [ ] 설정 적용 확인 (5-10분 대기)
- [ ] 사이트에서 정상 작동 확인

## 📚 참고 자료

- [Google Cloud API Key 제한 가이드](https://cloud.google.com/docs/authentication/api-keys#restricting_apis)
- [Firebase 보안 모범 사례](https://firebase.google.com/docs/projects/api-keys)
- [Vercel 도메인 확인](https://vercel.com/docs/concepts/projects/domains)

